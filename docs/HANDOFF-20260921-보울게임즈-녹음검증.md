# 보울게임즈 녹음 자동화 실전 검증

2026-09-21 보울게임즈 녹음 2건으로 접수부터 고객 화면 반영까지 검증했다.

## 완료

- 고객사 연결: `수집대기/보울게임즈` → `bowlgames`
- 원본 파일명은 바꾸지 않고 앞의 `YYMMDD_`를 실제 통화일로 사용
- 녹음 2건 전사 및 회의록 생성 성공
- Notion 커뮤니케이션보드에 2026-09-03·2026-09-11 기록 생성
- 두 기록의 `고객 공개`를 켜고 비공개 고객페이지 DB에 반영
- 원격 최신 보울게임즈 화면 데이터에서 두 날짜의 회의록 2건 확인
- 로컬 처리 장부의 두 작업을 `complete`로 종료

회의록 기본 모델은 Gemini API가 권장하고 실제 성공한 `gemini-3.6-flash`로
조정했다. 일시적인 API 실패는 다음 실행에서 회의록 생성부터 이어서 재시도한다.
형식 검사에서 멈춘 항목은 자동 재시도하지 않고 PM 확인 대상으로 유지한다.

## 공개 운영 결정

2026-09-21 사용자 결정에 따라 새 회의록은 Notion에 `고객 공개=true`로 만든다.
시스템은 고객페이지 반영을 요청하고, 각 PM은 Notion에서 내용을 수정한다.
숨겨야 하는 기록은 PM이 `고객 공개`를 해제한다.

자동화의 고객페이지 갱신 요청은 비공개 Worker의 `/ops/rebuild`를 사용한다.
브라우저에는 자동화 토큰을 전달하지 않는다. 비공개 Worker에
`AUTOMATION_TOKEN`을 secret으로 등록했고, 실제 요청 → 대기열 → 빌드 완료까지
검증했다.

## Drive 동기화

이 PC의 Drive Desktop 일시중지 표식을 제거하고 프로세스 실행을 확인했다.
이후 Drive 웹과 이 PC의 동기화 폴더를 대조했다. 같은 이름의 `수집대기`가
두 개지만 실제 자동화가 읽는 폴더에는 보울게임즈 녹음 2건이 있고,
다른 폴더는 비어 있다. 운영 접수함의 직접 링크를 확인했다. 중복 폴더는
삭제·이동하지 않았다. 새 녹음은 이름 검색이 아니라 직접 링크로 올린다.

이번 검증은 사용자가 선택해 업로드한 동일한 로컬 원본 사본을 임시 접수함에서
처리했다. 이 PC의 예약 폴더에는 당시 처리 장부가 없었으므로, 실제 접수함의
두 녹음과 기존 Notion 기록의 파일 지문이 각각 정확히 일치함을 확인하고
이 PC의 처리 장부에 완료 상태를 복원했다. 예행연습에서 신규 대상 0건이다.

## 검증 결과

- Python 테스트: 78개 통과
- Worker 테스트: 347개 통과
- 비공개 Worker 운영 버전: `28a9f849-a06c-46f1-960c-c441a8272054`
- 자동 갱신 API 실전 검증: `bowlgames` 대기열 `completed`, 오류 없음
- 원격 보울게임즈 최신 화면: 소통 7건, 이번 녹음 날짜 2건 포함

## 남은 일

1. PM이 새 업무 녹음 1건을 운영 접수함에 올린 뒤, 다음 예약 회차에서
   노션과 고객 화면까지 무인 반영되는지 확인한다.
2. 비어 있는 중복 폴더의 정리는 소유자와 필요성을 확인한 뒤 별도로 결정한다.

## 화면 개선 기록

- 완료: 제목이 없는 첫 공지 섹션의 첫 `heading` 줄을 섹션 제목으로 올려,
  첫 번째와 두 번째 섹션이 모두 같은 토글로 보이게 했다.
- 완료: 보울게임즈 공지 초안·게시본의 `보울게임즈 벤처기업인증`을 첫 섹션
  제목으로 교정하고 비공개 고객페이지에 반영했다.
- 완료: 새 녹음의 회의록 생성 지시문에 배경·논의·결정 근거·남은 확인 사항을
  구체적으로 적도록 보강하고, 화면 연결 과정의 인증정보는 제외하도록 했다.
  기존 9월 3일·11일 녹음도 사용자 승인 후 다시 전사하여 내용을 대조했다.
  두 Notion 회의록은 상세한 논의·결정·후속 작업으로 갱신했다. 기존 본문이
  자동 초안과 같아 PM의 별도 본문 수정은 없었으며, 교체 전 본문은 로컬에
  백업했다. 9월 11일 자금 조달 목표의 합계도 4.5억 원으로 바로잡았다.
  보울게임즈 비공개 고객 데이터 재빌드 후 원격 최신본에서 두 날짜의 본문이
  각각 HTML 2,936자·3,205자로 반영된 것을 확인했다. 고객 화면은 비밀번호
  입력이 필요해 이번 갱신 후 육안 검수는 아직 하지 못했다.
- 완료: 보울게임즈 공개 메일 5건에는 줄바꿈이 저장돼 있었다. 고객 화면에서
  이를 접던 서식을 수정했고, 새 HTML 메일은 목록 항목 뒤 줄바꿈도 보존한다.
- 완료: 보울게임즈 데이터룸에 담당자 편집 진입과 📌 고정을 연결했다.
  카드의 노션 게시 사이트 주소 표시는 「바로가기」다. 고정된 카드는 데이터룸과
  사이드바 「즐겨찾기」에 표시된다. 고정할 자료의 선택은 PM이 한다.

## 2026-09-22 회의록 문체 교정

- 9월 3일·11일 자동 생성 회의록의 긴 서술형 불릿을 `쟁점: 사실·수치 / 판단·미결` 형식으로 교정했다.
  안건 순서와 금액·날짜·계약 조건, 마지막 `항목 | 담당 | 기한` 표는 유지했다.
- 두 Notion 본문을 수정 전에 로컬 비공개 폴더에 백업했다. 원본 녹음을 다시 전사하지 않고,
  이미 기록된 사실을 읽기 쉬운 실무 메모로 재구성했다.
- 노션 본문 2건을 읽어 교체 내용을 확인하고, 보울게임즈 비공개 고객 데이터를 즉시 갱신했다.
  실제 고객 화면의 소통 내역에서 새 안건명·본문·후속 실행 표를 확인했다.
- 이후 새 녹음은 `src/summarize_transcript.py`가 같은 형식을 요구한다. `~다.`·`~습니다.`로
  끝나는 불릿은 자동 공개 전에 형식 검사에서 멈추고 PM 확인 대상으로 둔다.
