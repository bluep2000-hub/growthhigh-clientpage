# growthhigh-clientpage

노션을 읽어 클라이언트별 정적 페이지를 만드는 저장소다.
빌드 산출물을 그대로 커밋해 GitHub Pages 로 내보낸다. 레포 루트가 곧 배포물이다.

## 처음 세팅

```bash
pip install -r src/requirements.txt
cp .env.example .env
```

레포에 없는 것은 `.env` 하나뿐이다. `NOTION_TOKEN` 만 채우면 빌드가 끝까지 돈다.

| 키 | 없을 때 |
|---|---|
| `NOTION_TOKEN` | 빌드 불가 |
| `BUILD_TOKEN` | 빌드 불가. 공지의 원본인 저장소를 읽을 수 없다 |
| `CALENDAR_ICS_URL` | 일정에서 미팅이 빠진다 |
| `IMAP_HOST` · `IMAP_USER` · `IMAP_PASS` | `--skip-imap` 으로 우회한다. 기존 소통 내역은 노션에서 읽는다 |
| `OWN_MAIL_EXTRA` · `GOOGLE_APPLICATION_CREDENTIALS` | 선택이다 |

## 자주 쓰는 명령

```bash
python src/publish.py --client 슬러그              # pull→검사→빌드→배포. 평소엔 이것만 쓴다
python src/publish.py --client 슬러그 --dry-run    # 빌드까지만. 커밋도 push 도 안 한다
python src/check_copies.py                        # 사본이 지금 원본 템플릿에서 나온 것인가
python -m http.server 8000 --bind 127.0.0.1       # 로컬 확인
```

`publish.py` 는 어디서든 걸리면 **배포까지 가지 않고 멈춘다** — pull 충돌, 사본 갈라짐,
평문 경고, 산출물 아닌 파일이 바뀌어 있음. **내용이 안 바뀌었으면 커밋하지 않는다**
(봉투의 salt·iv 가 매번 무작위라 파일 바이트만으로는 판단할 수 없어, 배포본을 풀어 견준다).

빌더를 직접 부를 일도 있다.

```bash
git pull                                          # 직접 부를 때는 먼저
python src/build_client.py --skip-imap            # 조건을 갖춘 클라이언트 전부
python src/build_client.py --client 슬러그 --dry-run     # 파일도 노션도 건드리지 않고 확인만
```

`--bind 127.0.0.1` 을 빼면 레포 루트가 같은 네트워크에 통째로 열린다.

## 새 클라이언트 추가

코드를 고칠 일이 없다. 노션에서 속성을 채우고 빌드를 돌리면
`c/{슬러그}.enc` · `{슬러그}/index.html` · `logo/{슬러그}.*` 가 자동으로 생긴다.

### 1. 노션 — 공유페이지 DB

「페이지 유형」에 `👨‍💼클라이언트페이지` 가 든 행이 대상이다.

빌드 대상이 되는 조건은 **둘**이다. 하나라도 비면 경고를 남기고 건너뛴다.

- `슬러그` — 주소가 된다. 영문 소문자·숫자·하이픈만 쓴다
- `기업 DB` 릴레이션 — 기업명·사업내용·주소가 여기서 온다

`접속 비밀번호` 는 빌드를 막지 않는다. 비어 있으면 **암호화 없이 평문으로**
만들어지고 빌드 끝에 경고가 뜬다. 이름과 달리 평문 비밀번호를 넣는다.

선택 속성이다.

- `드라이브 URL` · `사업계획서 URL` — 없으면 좌측 「자료」 항목이 흐려진다
- `공지 DB` (URL) — **아직 이사하지 않은 기업만** 쓴다. 저장소에 그 기업의 공지가
  한 건이라도 생기면 빌드는 이 속성을 더 보지 않는다
- 페이지 아이콘 — 로고로 쓴다. 없으면 기업명 앞 두 글자를 대신 넣는다

공지는 노션에서 지정하지 않는다. 담당자가 클라이언트 페이지에서 「＋ 새 공지」로
만들고, 그것이 중계 서버의 저장소에 들어간다 — 그 기업의 첫 공지도 마찬가지다.
저장소에 공지가 생기기 전까지만 노션 `공지 DB` 를 읽는다.

프로젝트와 일정은 따로 지정하지 않는다.
프로젝트 DB 의 `고객사 정보` 가 그 기업 페이지를 가리키면 자동으로 붙고,
캘린더는 일정 제목이 `[기업명]` 으로 시작하면 자동으로 잡힌다.

**주요 소통은 담당자가 짚는다.** `소통 내역` DB 의 `주요` 체크박스를 켜면
그 건이 「소통 내역」 화면 맨 위 「주요 소통」으로 올라가고, 요약 리포트의
다섯 줄도 그것부터 채운다. 목록에서는 빠지지 않고 ★ 가 붙는다.

기계가 대신 추려 올리지 않는다. 「주요」라고 적힌 자리에 기계가 고른 것이
앉으면 그 표시가 값을 잃는다 — 읽는 사람은 그것을 사람이 짚어 준 것으로
읽는다. 시간순으로 늘어놓기만 해서는 우리가 한 미팅의 값어치가 전달되지
않는다는 것이 이 칸을 만든 까닭이다. 하나도 켜지 않으면 덩어리째 안 뜨고,
화면은 지금까지와 같다.

**미팅에서 나온 것을 기업 정보 화면으로 끌어올린다.** 따로 지정하지 않는다 —
둘 다 이미 있는 것에서 나온다.

- **미팅 리듬 한 줄** — 요약 타일 밑에 「함께한 미팅·통화 12회 · 최근 3개월
  4회 · 마지막 9월 5일」. 정기적으로 만나 쌓아 온 것 자체가 값어치인데
  화면에 그 리듬이 없었다. 최근 3개월이 0회면 그 대목만 빠진다
- **할 일** — 회의록 끝의 `| 항목 | 담당 | 기한 |` 표에서 나온다. **「일정」 카드
  안**, 다가오는 일정 아래에 이어 선다. 줄을 누르면 소통 내역의 그 회의록으로
  내려가 펴진다. 상태는 `할 일` DB 에 산다(아래)

카드를 따로 세우지 않는다. 미팅과 마감과 할 일은 담당자에게 다 「언제 무엇을」
이라, 갈라 두면 같은 물음의 답을 화면 두 곳에서 찾아야 한다.

**기한이 날짜로 읽히면 달력에 점이 찍힌다.** `2026-09-30` · `9/30` · `9월 30일`
을 알아본다. 연도가 없으면 그 할 일을 정한 해로 보고, 그보다 앞서면 이듬해로
민다. 왼쪽에 `D-9` 가 서고 일주일 안이면 색이 붙는다. **읽히지 않는 기한은
적힌 말 그대로 목록에만 둔다** — 「취득 진행 전」을 달력에 찍으려면 없는 날짜를
지어내야 한다. 회의록의 기한은 열에 아홉이 이런 말이라, 달력에 뜨게 하려면
담당자가 할 일 DB 의 `기한` 을 날짜로 고쳐 적어야 한다.

할 일은 **자르지 않는다.** 일정 목록은 다섯 건까지만 보이지만(공고 마감이
끝없이 들어온다), 몇 건이 서 있을지는 담당자가 「진행」으로 정하는 것이라
화면이 임의로 잘라 두면 올린 것이 소리 없이 사라진다.

표를 알아보는 것은 **머리 행 세 낱말**이다. 섹션 제목으로 찾지 않는다 —
같은 표가 「## 14. Action Items」로도 「## 3. 향후 일정 및 Action Items」로도
와서 번호도 말도 흔들린다. 회의록의 다른 표(비용·시나리오 비교)는 머리 행이
달라 걸리지 않는다. 이 형식은 `skills/talk-summary/SKILL.md` 가 정한 것이라,
거기를 고치면 여기도 함께 고쳐야 한다.

**표는 회의록 안에도 그대로 남는다.** 위로 올린 것은 그것을 가리키는 것이지
대신하는 것이 아니다 — 회의록은 그날의 기록이라 사후에 덜어 내지 않는다.

**할 일의 상태는 노션 `할 일` DB 에 산다.** 회의록은 그날 정해진 사실이라
사후에 고치지 않는다. 그러니 「그래서 그것이 지금 어떻게 되었는가」는 따로
살아야 한다. 회의록 표에 완료 칸을 하나 더 두는 길도 있었지만, 그러면 지난
회의록이 계속 바뀌어 어느 쪽도 못 믿게 된다.

```
회의록의 「항목·담당·기한」 표
   → 빌드가 뽑아 할 일 DB 에 「검토대기」로 앉힌다
   → 담당자가 「진행」으로 옮긴다
   → 그것만 클라이언트 화면에 나간다
```

**새로 들어온 줄은 화면에 안 나간다.** 기계가 뽑아낸 것이 사람 눈을 거치지
않고 클라이언트에게 보이는 일을 만들지 않는다 — 소통 DB 가 새 메일을
「검토대기」로 받는 것과 같은 셈법이다. **처음 한 번은 담당자가 쌓인 것을
훑어야** 화면에 뜬다.

빌드는 **없는 것만 넣고 있는 것에는 손대지 않는다.** 상태는 담당자의 것이라,
빌드가 돌 때마다 되돌려 놓으면 그 칸이 아무 뜻도 갖지 못한다. 같은 할 일이
다음 미팅에서 다시 확인되면 **공백을 지운 항목 글자가 똑같은 것만** 이미
있는 것으로 본다 — 말이 달라진 것까지 같다고 보면 서로 다른 할 일이 소리
없이 하나로 뭉친다. 이미 「완료」로 내린 것이 다시 나와도 되살아나지 않는다.
정말 다시 열린 것이면 담당자가 상태를 되돌린다.

`기한` 은 날짜가 아니라 **글자**다. 실제 값이 「미정」·「다음 미팅 전」·
「자료 전달 시」·「취득 진행 전」이라, 날짜 칸으로 두면 없는 날짜를 지어내야 한다.

「완료」는 수만 센다. 무엇을 끝냈는지가 아니라 얼마나 끝냈는지가 읽는 사람에게
뜻이 있다. 「보류」는 통째로 감춘다.

담당이 「그로스하이」인 것도 클라이언트에게 그대로 보인다. **우리가 무엇을
하고 있는지를 전하는 것이 이 목록의 값어치**라서다. 회의록은 지금도 검토
없이 그대로 나가지만(`skills/talk-summary/SKILL.md`), 접힌 줄 안에 있어
사실상 안 읽혔다 — 위로 올린 뒤로는 읽힌다. 회의록에 적는 것의 무게가
그만큼 늘었다.

### 2. 빌드

```bash
python src/publish.py --client 새슬러그 --dry-run
```

무엇이 나올지 먼저 본다. 비밀번호가 비어 있으면 여기서 멈추고 알려 준다 —
평문이 배포되는 일은 `publish.py` 가 막는다.

### 3. 배포

```bash
python src/publish.py --client 새슬러그
```

1~2분 뒤 `https://bluep2000-hub.github.io/growthhigh-clientpage/{슬러그}/` 로 열린다.

## 반드시 지킬 것

**작업 전 `git pull`.** 빌드 산출물을 커밋하는 구조라 두 PC 에서 같은 클라이언트를
빌드하면 충돌한다.

**루트 `index.html` 을 고쳤으면 반드시 재빌드.** `{슬러그}/index.html` 은 루트
`index.html` 의 사본이다. 루트만 고치고 push 하면 클라이언트 주소는 옛 화면 그대로다.
빌드가 사본을 다시 만들어 준다.

빌드가 `index.html 에서 주입 지점을 찾지 못했습니다` 경고를 내면 사본이 아예
만들어지지 않은 것이다. 빌드는 `</main>` 다음 빈 줄 다음 `<script>` 를
주입 지점으로 쓴다. 그 사이에 무엇을 끼워 넣으면 안 된다.

**서비스 계정 키·비밀키를 레포 안에 두지 않는다.** 레포 루트는 통째로 웹에 서빙된다.
`worker/` 도 마찬가지다 — 소스가 공개되는 것은 무해하지만 값은 아니다.

## 담당자가 공지를 고친다

담당자는 클라이언트 페이지에서 공지를 그 자리에서 고친다. 노션에 들어가지 않는다.

```
https://client.growthhigh.co.kr/{슬러그}/?edit
```

`?edit` 를 달아야 「담당자 편집」 스위치가 뜬다. 안 달면 클라이언트가 보는 화면
그대로다 — 편집이라는 것이 있다는 사실도 보이지 않는다. **담당자 공용 비밀번호**는
클라이언트 비밀번호와 다른 값이고 Cloudflare 에만 있다.

공지의 원본은 **중계 서버의 저장소(Cloudflare D1)** 다. 노션이 아니다.

| | |
|---|---|
| 내용 수정 | 줄 아무 데나 클릭해 바로 타이핑 |
| 줄 사이 이동 | ↑ ↓ — 마우스로 찍지 않아도 된다. 여러 줄로 접힌 줄은 그 안에서 먼저 움직인다 |
| 실행 취소 | **Ctrl+Z** 한 걸음씩, **Ctrl+Shift+Z** 다시 앞으로. 글자도 옮긴 것도 지운 것도 함께 물린다 |
| 서식 | 글자를 고르면 뜨는 막대 — 굵게·기울임·밑줄·취소선·코드·링크 |
| 서식 단축키 | Ctrl+**B** 굵게 · **I** 기울임 · **U** 밑줄 · **E** 코드 · **K** 링크 · Ctrl+Shift+**S** 취소선 |
| 블록 유형 단축키 | **Ctrl+Shift+0~8** — 0 본문 · 1~3 제목 · 4 할 일 · 5 글머리 · 6 번호 · 7 토글 · 8 코드. 노션과 같은 짝이다 |
| 치면서 서식 | `**굵게**` `*기울임*` `` `코드` `` `~~취소~~` — 닫는 글자를 치면 표시가 사라지고 서식이 된다 |
| 줄 복제 | **Ctrl+D** |
| 링크 걸기 | 글자를 고른 채로 **주소를 붙여넣으면** 그 글자가 링크가 된다 |
| 글자 색 | 그 막대의 「🎨」 — 윗줄이 글자 색, 아랫줄이 형광펜. 「✕」가 색 없애기 |
| 여러 줄 선택 | 줄에 걸쳐 끌거나 **Shift+↑ ↓** 로 잡는다. **Tab · Shift+Tab · Ctrl+Shift+↑ ↓ · Backspace** 가 잡힌 모두에 걸린다 |
| 줄 안에서 줄바꿈 | Shift+Enter |
| 줄 추가 | 줄 끝에서 **Enter**, 또는 **줄 아래 빈 자리를 클릭**. 단추는 없다 |
| 줄 나누기 | 줄 가운데서 **Enter** — 커서 뒤가 새 줄로 내려가고 유형을 물려받는다 |
| 줄 병합 | 줄 처음에서 **Backspace** — 유형을 벗고, 내어쓰고, 그 다음에 윗줄에 붙는다 |
| 줄 삭제 | 줄에 마우스를 얹으면 나오는 ✕, 또는 빈 줄에서 Backspace |
| 붙여넣기 | 여러 줄을 붙이면 **줄 수만큼 줄이 된다.** `- ` `1. ` 로 시작하는 줄은 그 유형이 된다 |
| 제목 | 화면 맨 위 제목을 클릭해 바로 타이핑. **Enter 로 첫 줄로 내려간다** |
| 줄 이동 | 줄 **왼쪽 드래그 핸들(⠿)를 끌거나**, **Ctrl+Shift+↑ ↓**, 또는 줄에 마우스를 얹으면 나오는 ↑ ↓ |
| 할 일 체크 | 체크 표시를 클릭. 켜면 클라이언트 화면에서도 끝난 일로 보인다 |
| 들여쓰기 | **Tab · Shift+Tab**, 또는 같은 자리의 ⇥ ⇤ |
| 단축키 안내 | 저장 바 밑의 「단축키 안내」 — 여기 적힌 것이 다 들어 있다 |
| 블록 유형 | **`/`** 로 목록을 부르거나, 줄 앞에서 **`- ` `1. ` `[] ` `[x] ` `> ` `" ` `# ` `## ` `### `**, 또는 Ctrl+Shift+숫자 |
| 표 | **`/표`** — 2×2 로 선다. 칸 사이는 **Tab**, 마지막 칸에서 Tab 이면 행이 는다. 행·열은 표 밑 단추로 늘리고 줄인다 |
| 코드 | **`/코드`**, 또는 **Ctrl+Shift+8**. 그 안에서는 Enter 가 줄바꿈이고 `- ` `/` 가 그냥 글자다 |
| 섹션 이름 | 섹션 머리말을 클릭해 바로 타이핑. **Enter 로 그 섹션 첫 줄로 내려간다** |
| 섹션 만들기 | **`/새 섹션`**, 또는 맨 아래 「＋ 새 섹션」 — 커서가 이름으로 간다 |
| 섹션 지우기·옮기기 | 섹션 머리말에 마우스를 얹으면 나오는 ↑ ↓ ✕ |
| 새 공지 | 제목 옆 「＋ 새 공지」 — 오늘 날짜로 만들고 커서가 제목으로 간다 |
| 이전 버전 | 저장 바의 「이전 버전」 — 저장 시각을 보고 골라 되돌린다 |

**키보드가 글에서 떠나지 않는다.** 줄 사이 이동·들여쓰기·옮기기·종류 바꾸기가
전부 키보드로 된다. 종류를 바꾸는 글자는 **노션과 같은 것**을 쓴다 — `- ` `* `
`+ ` 는 글머리, `1. `(숫자 아무거나)은 번호, `[] ` 는 할 일(`[x] ` 는 켜 둔 채로),
`> ` 는 토글, `" ` 는 인용, `# ` `## ` `### ` 는 제목이다. 줄 **앞에서** 쳤을
때만이고, 문장 가운데에서 친 것은 그냥 글자다.

**종류를 숫자로도 바꾼다.** `Ctrl+Shift+0~8` 은 **노션과 같은 짝**이다 — 담당자가
이미 손에 익힌 것이라 새로 외울 것이 없다. 노션의 9(새 페이지)는 여기 없고,
인용에는 노션에도 숫자가 없어 `" ` 와 `/` 로만 간다. 여러 줄을 잡아 놓고 누르면
잡힌 모두에 걸린다.

`e.key` 가 아니라 `e.code` 를 본다 — Shift 를 쥐면 `1` 이 `!` 로 오고, 그 글자가
자판마다 다르다.

**줄 안에도 제목을 둔다.** 섹션이 그 공지의 큰 제목이고, `# ` `## ` `### `
는 그 아래 세 단계다 — 섹션보다 커지면 위계가 뒤집히므로 셋 다 섹션 이름보다 작다.
저장소가 받아 주는 종류가 늘어난 것이라 **중계 서버를 먼저 올리고 페이지를
다시 지어야 한다.** 순서가 뒤바뀌면 담당자의 저장이 「모르는 종류」로 거절된다.

**여러 줄을 한꺼번에 잡는다.** 줄에 걸쳐 끌면 그 줄들이 옅게 칠해지고,
Tab · Shift+Tab · Ctrl+Shift+↑ ↓ · Backspace 가 잡힌 모두에 걸린다. 안에 든
줄은 그릇이 데려가므로 함께 잡혔어도 따로 움직이지 않는다. 글자 서식은
여러 줄에 걸리지 않는다 — 편집기가 남의 칸에는 손을 못 대 반만 먹힌다.
그래서 두 줄에 걸쳐 고르면 서식 막대가 아예 뜨지 않는다.

**Ctrl+Z 가 한 걸음씩 물린다.** 저장 바의 「되돌리기」는 손댄 것을 통째로
물리는 것이고, 이것은 방금 한 하나만 물린다 — 잘못 누른 Tab 하나 때문에
반나절 적은 것을 버리지 않아도 된다. 글자도 옮긴 것도 지운 것도 종류 바꾼
것도 같은 줄에 선다. **저장하고 나면 걸음을 비운다** — 저장 뒤 화면의 주소는
저장소가 지어 준 것이라, 그 앞으로 돌아가면 없는 주소를 되살려 내보낸다.

걸음마다 공지 화면과 장부를 통째로 떠 둔다. 손질마다 되돌리는 짝을 따로
쓰지 않는 까닭은, 그렇게 하면 새 손질을 만들 때마다 짝 하나를 잊게 되고
잊은 자리는 조용히 어긋나기 때문이다 — 저장이 문서를 통째로 보내는 것과
같은 셈법이다.

**빈 글머리에서 Enter 는 목록을 빠져나온다.** 들여쓴 줄이면 한 칸 나오고,
그다음 본문이 된다. 그러지 않으면 빈 글머리가 끝없이 이어진다.

**치면서 서식이 된다.** `**굵게**` `*기울임*` `` `코드` `` `~~취소~~` 를
닫으면 표시 글자가 사라지고 서식이 입혀진다. 태그를 직접 심는다 —
`execCommand` 는 「지금 굵은가」를 화면에 그려진 대로 보므로, 이미 굵은 제목
줄 안에서는 굵게가 **굵기를 푸는 일**이 되어 버린다.

서식을 닫으면 커서가 그 서식 **바깥**으로 나온다. 안에 둔 채로 두면 뒤에
치는 글이 전부 딸려 굵어지고, 담당자는 끄는 법을 따로 찾아야 한다.

**줄을 만드는 것이 단추가 아니라 글 쓰는 일 자체다.** 가운데서 Enter 를 치면
줄이 갈라지고, 줄 처음에서 Backspace 를 치면 도로 붙고, 회의록을 통째로
붙여넣으면 줄 수만큼 줄이 되고, `/` 한 글자가 종류 목록을 부른다.
「＋ 줄 추가」 단추는 **없앴다** — 줄 아래 빈 자리를 누르면 줄이 선다.

**열면 커서가 놓일 자리가 이미 있다.** 빈 공지에도 빈 줄 하나가 서 있고,
「글을 쓰거나 / 를 눌러 종류를 고르세요」가 그 자리에 흐리게 떠 있다.
그전에는 처음 보이는 것이 글이 아니라 「섹션 이름 · ＋ 줄 추가 · ＋ 새 섹션」
이라는 서식이었다. 이 빈 줄은 저장에 나가지 않고 「바뀐 것」으로 세지도 않는다.

**제목 → Enter → 첫 줄 → Enter → 다음 줄.** 새 공지 한 건을 단추 한 번 누르지
않고 끝까지 적는다(저장만 빼고). 섹션도 마찬가지다 — `/새 섹션` 으로 세우고, 이름을
치고, Enter 로 그 안으로 들어간다.

「섹션 이름」은 손이 갈 때만 뜬다. 이름은 안 붙여도 된다.

`/` 목록은 줄 처음이나 빈칸 다음의 `/` 에서만 뜬다 — 주소를 치다 나오는 `/` 는
글자일 뿐이다. 이어서 치면 추려지고(`/할` → 할 일), ↑ ↓ 로 고르고 Enter 로
넣는다. 목록에 없는 것을 치면 조용히 닫힌다.

**한글을 짓는 중에는 이 단축키가 하나도 걸리지 않는다.** 「ㅎㅏㄴ」을 맺는
Enter 까지 줄 쪼개기로 읽으면 한 글자 칠 때마다 줄이 갈라진다.

**손댄 것은 「저장」을 눌러야 나간다.** 고치기도 보태기도 지우기도 마찬가지다.
그전까지는 화면에만 쌓인다 — 고친 줄과 보탠 줄에는 왼쪽에 표시가 붙고, 지울
줄은 그어진 채로 남아 그 자리에서 「되돌리기」가 된다. 저장 바는 무엇이 몇 줄
나갈지를 갈라서 센다(「제목 · 2줄 수정 · 1줄 추가 · 1줄 이동 · 1줄 체크 · 1줄 삭제」). 잘못 쳤으면
「되돌리기」로 한꺼번에 물린다. 저장하지 않고 화면을 떠나려 하면 붙잡는다.

**한 번에 저장할 수 있는 손질에 상한이 없다.** 공지 문서를 통째로 보내므로
통째로 들어가거나 통째로 거절된다 — 반쯤 저장된 상태가 생기지 않는다.

보탠 줄은 **화면에서 놓은 자리 그대로** 들어간다.

「＋ 새 공지」는 **오늘 날짜로 빈 공지 한 건을 만든다.** 날짜를 고르는 창은 없다 —
미팅 직후 기록이 목적이라 거의 언제나 오늘이다. 빌드가 가장 최근 공지 하나만
페이지에 올리므로, **다음 빌드부터 그 공지가 클라이언트에게 보이는 것이 된다.**
누르면 확인창이 그것부터 말한다. 지난 공지는 저장소에 그대로 남는다.
만드는 것만으로는 재빌드가 걸리지 않는다 — 빈 공지가 나가면 클라이언트 화면에서
공지가 통째로 사라지기 때문이다. 첫 줄을 적고 「저장」을 누를 때 함께 나간다.

**공지가 하나도 없는 기업도 페이지에서 첫 공지를 만든다.** 노션을 열 일이 없다.

**섹션도 페이지에서 만든다.** 미팅에서 새 주제가 나오면 `/새 섹션` 으로
칸부터 세우고 그 안에 적는다. 이름을 고치고, 순서를 바꾸고, 지운다. 줄이 든 칸을
지우려 하면 몇 줄이 함께 사라지는지 먼저 말하고 묻는다. 마지막 칸을 지우면 빈 칸
하나가 자리에 남는다 — 적을 곳이 없어지지 않게.

**끌어서 옮긴다.** 줄에 마우스를 얹으면 왼쪽에 드래그 핸들(⠿)가 나온다. 놓이는
자리는 **가리킨 줄의 위나 아래, 그 줄과 같은 깊이**다 — 가로 자리로 깊이까지
정하지 않는다. 놓을 자리가 두 가지 뜻을 가지면 담당자는 어디에 떨어질지
모른 채 손을 놓게 된다. 깊이는 Tab 이다. 안에 든 줄은 통째로 딸려 가고,
제 자손 안으로는 들어가지 않는다. 접어 둔 칸의 줄 옆에는 놓이지 않는다.

**편집 모드에서는 칸이 접히지 않는다.** 접는 것은 읽는 사람을 위한 것이지
고치는 사람을 위한 것이 아니다 — 고치는 동안에는 다 보여야 하고, 접힌 칸에
커서를 두면 보이지 않는 곳에서 글이 써진다. 머리말을 눌러도 접히지 않는다:
이름을 고치러 누른 손이 칸을 통째로 접으면 방금까지 보던 줄이 사라진다.
클라이언트 화면에서는 지금까지대로 접힌다.

**줄마다 붙던 종류 고르개를 걷었다.** 종류를 바꾸는 길이 셋이나 되고(`/`,
줄 앞 글자, Ctrl+Shift+숫자) 전부 손이 글에서 떠나지 않는데, 줄마다 고르개가
달려 있으면 읽는 데 방해가 되기만 한다. 이동·들여쓰기 버튼과 드래그 핸들은
그대로 둔다 — 그것은 형식이 아니라 자리를 정하는 것이다.

**단축키 안내가 화면 안에 있다.** 저장 바 밑에 접혀 있고, 편집 모드에서만
그려지므로 클라이언트는 못 본다. 담당자가 쓰다 막혔을 때 찾아갈 곳이 화면
안에 있어야 한다 — 이 파일을 열어 보라고 할 수는 없다.

**옮기기와 종류 바꾸기에 제약이 없다.** 안에 든 줄이 몇 개든 ↑ ↓ 로 옮기고,
안엣것은 통째로 딸려 간다. 종류를 바꿔도 그 줄은 제자리에 그대로 있다 —
지우고 다시 만드는 것이 아니라 값 하나를 고치는 일이기 때문이다. 한 줄에
고치기·옮기기·종류 바꾸기를 함께 걸어도 된다. 들여쓰기 깊이에도 제한이 없다.

**종류를 바꾸면 그 자리에서 모양이 달라진다** — 글머리는 점, 번호는 번호,
본문은 표시 없이, 인용은 왼쪽 줄, 할 일은 체크, 토글은 접힘. 저장하기 전에도
클라이언트가 보게 될 모양 그대로다. 클라이언트 화면을 그리는 표를 편집 모드가
그대로 쓰므로 다음 빌드와 어긋나지 않는다. 안에 든 줄을 접는 토글만은 뼈대가
달라 모양이 그대로 있고, 대신 「종류 → …」 딱지가 무엇으로 바뀔지 알린다.

**잠긴 항목이 없다.** 노션이 쓰기를 받아 주지 않던 것이 없어지면서 그 개념이
함께 사라졌다.

**표는 칸을 한 줄로 늘어놓고 `cols` 로 끊는다.** 행을 따로 두지 않는다 — 행이
아무것도 지니지 않아서, 두면 확인할 것과 주소를 붙일 것만 한 겹 늘고 담당자에게
보이는 것은 달라지지 않는다. 저장소가 **네모가 아닌 표를 거절한다**(칸 수가
`cols` 의 배수가 아니면 422). 한 번 어긋난 표는 스스로 낫지 않기 때문이다.
빈 칸은 그대로 싣는다 — 빈 줄과 달리 비워 둔 칸은 「없는 것」이 아니라
「비워 둔 것」이라, 빠뜨리면 그 줄만 칸이 모자란다.

표는 **첫 행을 머리로 그린다.** 따로 지정하지 않는다 — 회의록의 표는 거의
언제나 첫 줄이 머리다. 한 줄에 여덟 칸까지다.

**코드는 서식 없는 글 한 덩어리다.** 줄바꿈을 `<br>` 로 싣는다. 그 안에서는
`- ` `# ` `/` 가 종류를 바꾸거나 목록을 부르지 않는다 — 코드에 흔한 글자를
명령으로 읽으면 코드를 적을 수가 없다.

노션에 있고 여기 아직 없는 것은 **그림과 첨부**다. 파일을 어디에 둘지가
정해져야 만들 수 있다 — 봉투는 암호화되지만 `assets/` 는 비밀번호 없이
열리는 자리라, 그 결정이 곧 「공지에 붙인 그림을 누가 볼 수 있는가」가 된다.

**저장소가 원본이다.** 페이지에서 고친 것이 곧 원본이므로 다음 빌드가 덮어쓰지
않는다. 저장하면 재빌드가 자동으로 걸려 1~2분 뒤 클라이언트에게 닿는다
(`.github/workflows/rebuild.yml`). 노션 공지 DB 는 지난 기록으로만 남는다 —
새로 쓰지도 읽지도 않는다.

다른 담당자가 그 공지를 먼저 저장했으면 **저장이 통째로 막히고** 저장소의 지금
내용이 나란히 뜬다. 어느 쪽을 남길지는 담당자가 고른다. 화면에 적어 둔 글은
그대로 남는다.

**「이전 버전」으로 되돌린다.** 저장할 때마다 직전 공지가 한 벌씩 남고, 공지
하나당 스무 벌까지 이고 간다. 저장 바의 「이전 버전」을 누르면 저장 시각과
그때의 제목이 뜨고, 하나를 고르면 그 판이 **새 판이 되어** 들어간다 — 되돌린
것도 다시 되돌릴 수 있고, 재빌드도 똑같이 걸린다.

중계 서버는 `worker/` 에 있다. 배포와 비밀값은 `worker/README.md` 를 본다.
결정의 이유는 `docs/adr/0004-공지-원본-이사.md` 를 본다
(`0003` 의 토대 한 줄을 그것이 뒤집는다).

## 코드가 기대하는 노션 속성명

이름을 바꾸면 조용히 빈 값이 된다.

| DB | 속성 |
|---|---|
| 공유페이지 | `페이지명` `슬러그` `기업 DB` `담당자` `업종` `접속 비밀번호` `드라이브 URL` `사업계획서 URL` |
| 기업 | `기업명` `사업내용` `주소` `설립연월` `업종분류` `인증ㅣ특허` |
| 프로젝트 | `프로젝트명` `프로젝트 유형` `프로젝트 상태` `진행기간` `최신로그` `확보 성과` `고객사 정보` |
| 소통 내역 | `제목` `일자` `채널` `방향` `요약` `클라이언트` `상태` `Message-ID` `주요` |
| 할 일 | `항목` `담당` `기한` `상태` `클라이언트` `정한 날` `출처` `키` |

`공유페이지` 의 `공지 DB` 는 표에서 빠졌지만 **아직 살아 있다.** 저장소에 그 기업의
공지가 아직 없을 때만 빌드가 그리로 간다 — 옮겨 가는 동안의 다리다. 다리가 걷히면
그 속성도 함께 걷는다. 지난 공지는 노션에 그대로 남는다.

`소통 내역` 의 `주요` 는 체크박스다. **속성이 없어도 빌드는 돈다** — 없는 것과
꺼 둔 것을 같게 보므로 「주요 소통」 덩어리가 안 뜰 뿐이다. 이름을 다르게 지으면
조용히 아무것도 안 뜬다.

`할 일` DB 의 `키` 는 **빌드가 쓰는 것이다.** 같은 할 일을 두 번 넣지 않으려는
열쇠라, 사람이 고치면 그 줄이 다음 빌드에 하나 더 생긴다.

**노션 통합을 DB 마다 따로 연결해야 한다.** 이 워크스페이스는 부모 페이지가
아니라 **DB 하나하나에** 「DB 업로드 파이프라인」 통합이 붙어 있다. 새 DB 를
만들면 상속되지 않으므로, 노션에서 그 DB 를 열고 `⋯ → 연결 → DB 업로드
파이프라인` 을 더해야 한다. 안 하면 빌드가 `노션 404` 경고를 내고 그 DB 몫만
빈 채로 지나간다.

`인증ㅣ특허` 의 가운데 글자는 세로줄(`|`)이 아니라 한글 모음 `ㅣ` 이다.

## Agent skills

### Issue tracker

이슈는 이 레포의 GitHub Issues 에서 추적한다 (`gh` CLI). `docs/agents/issue-tracker.md` 참고.

### Triage labels

기본 5개 라벨을 그대로 쓴다 — `needs-triage` `needs-info` `ready-for-agent` `ready-for-human` `wontfix`. `docs/agents/triage-labels.md` 참고.

### Domain docs

single-context — 루트에 `CONTEXT.md` 하나, `docs/adr/` 하나. `docs/agents/domain.md` 참고.
